# 開発環境用のオーバーライドファイル例
# このファイルを docker-compose.override.yml にコピーして使用してください
# docker-compose.override.yml は自動的に読み込まれます（.gitignoreに含める）

services:
  backend:
    # 開発環境: 追加の環境変数や設定をここに記述
    environment:
      # 開発用の追加設定例
      # DEBUG: "true"
      # LOG_LEVEL: "debug"
    volumes:
      # 開発環境: 追加のボリュームマウント例
      # - ./backend/.env.local:/app/.env.local:ro
    # 開発環境: ポートフォワーディングの追加例
    # ports:
    #   - "8001:8001"  # デバッグポートなど

  db:
    # 開発環境: データベースの追加設定例
    environment:
      # POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=ja_JP.UTF-8"
    # 開発環境: データベースのバックアップボリューム例
    # volumes:
    #   - ./backups:/backups

  frontend:
    # 開発環境: フロントエンドの追加設定例
    volumes:
      # 開発環境: ソースマップの有効化など
      # - ./nginx.dev.conf:/etc/nginx/conf.d/default.conf:ro
