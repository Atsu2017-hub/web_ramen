{
  "name": "web-ramen",
  "version": "1.0.0",
  "description": "本格ラーメン屋の予約・注文システム",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --coverage",
    "test:run": "vitest run",
    "build": "node scripts/build.js",
    "build:clean": "node scripts/build.js --clean"
  },
  "devDependencies": {
    "vitest": "^2.1.3",
    "@vitest/ui": "^2.1.3",
    "@vitest/coverage-v8": "^2.1.3",
    "jsdom": "^25.0.1"
  }
}

